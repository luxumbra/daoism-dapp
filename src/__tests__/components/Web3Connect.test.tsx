import React from 'react';

import { screen } from '@testing-library/react';

import { Web3Connect } from '@daoism/components/Web3Connect';
import { render } from '@daoism/test/utils';

test('renders the Web3Connect component', () => {
  // render(<Web3Connect />);
  // const web3c = screen.findByTestId('web3c');
  // expect(web3c).toBeInTheDocument();
});
