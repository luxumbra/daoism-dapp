import { screen } from '@testing-library/react';

import { DesktopMenu } from '@daoism/components/SiteNav';
import { render } from '@daoism/test/utils';

test('renders the DesktopMenu', () => {
  // render(<DesktopMenu />);
  // const desktopMenu = screen.findByTestId('desktop-menu');
  // expect(desktopMenu).toBeInTheDocument();
});
