import React from 'react';

import { screen } from '@testing-library/react';

import Transfer from '@daoism/components/Transfer';
import { render } from '@daoism/test/utils';

test('renders the Transfer component', () => {
  // render(<Transfer />);
  // const transfer = screen.findByTestId('transfer');
  // expect(transfer).toBeInTheDocument();
});
